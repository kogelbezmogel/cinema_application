import {createTheme} from "@mui/material";

export const darkTheme = createTheme({
    palette : {
        primary : { main : '#0a0a0a' },
        secondary : {main : '#855000'},
        background : { default : '#424242', paper : '#2e2e2e' }
    }
});